name: CI with Codacy

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  codacy-security-scan:
    name: Codacy Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Codacy Analysis CLI
        uses: codacy/codacy-analysis-cli-action@master
        with:
          project-token: ${{ secrets.CODACY_PROJECT_TOKEN }}
          verbose: true
          output: results.sarif
          format: sarif
          gh-code-scanning-compat: true
          max-allowed-issues: 2147483647

      - name: Upload SARIF results file
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
```

**Key change:** Line with `upload-sarif@v2` → change to `upload-sarif@v3`

## Steps to Apply the Fix

1. **Edit the workflow file** in your repository:
   - Go to `.github/workflows/ci.yml`
   - Click the pencil icon to edit
   - Change `@v2` to `@v3` in the "Upload SARIF results file" step

2. **Commit the change**:
```
   Update CodeQL action to v3
